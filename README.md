# CLI Chatbot 🤖

A simple command-line chatbot built using the OpenAI API and Python.

## 🧠 Features
- Uses OpenAI GPT models for responses.
- Reads API key from `.env` file.
- Simple and fast CLI interface.

## ⚙️ Setup

1. Clone the repo:
   ```bash
   git clone https://github.com/AdityaAjay07/cli-chatbot.git
   cd cli-chatbot
